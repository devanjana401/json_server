{
  "name": "plantify-json",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
        "start": "json-server --watch gardening.json --port $PORT --static ./images",

            "dev": "json-server --watch gardening.json --port 3000 --static ./images",


    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
